#include<stdio.h>
#include<conio.h>
int main()
{
    int l,b,h,V;
    printf("Enter the value of length,breadth and height : ");
    scanf("%d%d%d",&l,&b,&h);
    V=l*b*h;
    printf("Volume of cubiod is %d",V );
    getch();
}
