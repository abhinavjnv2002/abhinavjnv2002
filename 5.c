#include<stdio.h>
#include<conio.h>
int main()
{
    int l,b,A;
    printf("Enter the length and breadth of rectangle : ");
    scanf("%d%d",&l,&b);
    A=l*b;
    printf("Area of ractangle is %d",A);
    getch();
}
